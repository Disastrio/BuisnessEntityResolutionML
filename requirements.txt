# Business Entity Resolution — runtime dependencies
# Python 3.10+ (validated on 3.13). Versions pinned to the tested environment.

numpy==2.3.4
pandas==3.0.5
scikit-learn==1.9.1        # required by LightGBM's sklearn interface
lightgbm==4.7.0
rapidfuzz==3.14.6
tqdm==4.70.1
optuna==5.0.0              # hyperparameter tuning (src/train.py)
